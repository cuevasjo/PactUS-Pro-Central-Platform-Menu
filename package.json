{
  "name": "pactus-pro-central-platform-menu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "validate:registry": "tsx scripts/validate-registry.ts",
    "test": "tsx scripts/test-registry.ts",
    "ci": "npm run typecheck && npm run validate:registry && npm test"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
